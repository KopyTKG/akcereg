name: FrontEnd Docker scout CI

on:
  pull_request:
    branches: [ "Live" ]

permissions:
  contents: read
  packages: write
  pull-requests: write
  
jobs:
  FrontEnd-scout-image:
    runs-on: ubuntu-latest
    env:
      REGISTRY: ghcr.io
      IMAGE_NAME: kopytkg/akcereg-fe
    steps:          
      - name: Check out code
        uses: actions/checkout@v3

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GHCR_PAT }}

      - name: Build and push
        id: build-image
        run: |
          docker build --file FrontEnd/DockerFile --tag {{ env.IMAGE_NAME }}:latest --load FrontEnd/.
           
      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: thekrewkopy
          password: ${{ secrets.DCKR_PAT }}
        
      - name: Docker Scout
        if: ${{ github.event_name == 'pull_request' }}
        uses: docker/scout-action@v1
        with:
          command: cves
          image: ${{ env.IMAGE_NAME }}
          write-comment: true
          github-token: ${{ secrets.GITHUB_TOKEN }}
